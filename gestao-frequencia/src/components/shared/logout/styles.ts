import styled from "styled-components";

export const Wrap = styled.div`
    display: 'flex';
    align-items: 'center';
    justify-content: 'center';
    margin: 0 auto;

    &:hover{
        cursor: pointer;
    }`